# Exercise 2:
# Create a puppet manifest:
#  - Named puppet.pp
#  - that puts the value of the 'puppet' key from hiera
#  - into /tmp/puppet.txt
# Apply with:
puppet apply --verbose --hiera_config=./hiera.yaml ./puppet.pp
cat /tmp/puppet.txt
